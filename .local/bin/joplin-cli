#!/bin/bash

# Launch joplin-cli
# See Docker files in .dotfiles/files/joplin-cli

set -euo pipefail

if ! type docker &> /dev/null; then
    echo "Docker not installed"
    exit 1
fi

image=nevillelyh/joplin-cli
arch=$(uname -m)
case $arch in
    aarch64)
        arch=arm64
        ;;
    x86_64)
        arch=amd64
        ;;
esac

docker pull -q $image:$arch > /dev/null

cmd="docker run -it -v joplin-cli:/root/.config/joplin --rm $image:$arch"
if tty &> /dev/null; then
    $cmd
else
    alacritty -e "$cmd"
fi
